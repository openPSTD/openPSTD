#ifndef STATE_H
#define STATE_H

/**
 * State of the main toolbar (i.e. which button is selected).
 */
enum State {
    SELECT,
    MOVE,
    ADDDOMAIN,
    ADDSOURCE,
    ADDRECEIVER,
    MEASURE
};

#endif